# Ride_Services-_Project
This project is a Ride Management REST API built with Django and Django REST Framework that enables administrators to manage rides, users, and ride events. It supports filtering, pagination, and sorting, including GPS-based distance sorting. The API is optimized using advanced Django ORM techniques to efficiently handle large datasets.
